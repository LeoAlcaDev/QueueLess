# En produccion (Vercel) el front se sirve por HTTPS y el backend (ALB) es HTTP, asi que el
# navegador no lo deja llamar directo. Apuntamos el API al mismo origen (/api) y vercel.json
# lo reenvia al backend del lado del servidor. No hay secretos aca, por eso se versiona.
VITE_API_URL=/api
